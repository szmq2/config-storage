zuul:
  # 代理前缀：名称
  prefix: /api
  # 代理前缀：是否在路由路径中去掉
  strip-prefix: true
  routes:
    rd-demo:
      path: /dop-demo/**
      serviceId: dop-demo

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true